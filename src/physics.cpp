#include "physics.hpp"
#include <glm/glm.hpp>
#include <glm/vec3.hpp>
#include <cstddef>
#include <vector>

void Physics::step(std::vector<SimObject> &simobjects, float dt) {
    for (auto & a : simobjects) {
        for (auto & b : simobjects) {

        }
    }
}
